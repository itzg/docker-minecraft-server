version: '3'
# Other docker-compose examples in /examples
##tst
services:
  minecraft:
    image: elanderholm/docker-minecraft-server
    ports:
      - "25565:25565"
    volumes:
      - "mc:/data"
    environment:
      EULA: "TRUE"
    restart: always
volumes:
  mc:
